import React from 'react';

const multipleImageList = () => {
    return (
        <div className="row m-3">
            <img src={} alt="Multiple image preview" />
        </div>
    );
};

export default multipleImageList;